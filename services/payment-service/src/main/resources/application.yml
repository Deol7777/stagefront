# payment-service config (skeleton). Datasource/Kafka added later.
spring:
  application:
    name: payment-service

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
